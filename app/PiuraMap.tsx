'use client';import{MapContainer,TileLayer,Marker,Popup,useMap}from'react-leaflet';import L from'leaflet';import'leaflet/dist/leaflet.css';
type Property={id:number;title:string;operation:string;price:number;coords:[number,number]};type Props={properties?:Property[];onSelect?:(p:Property)=>void};const piura:[number,number]=[-5.1945,-80.6328];const icon=(price:number)=>L.divIcon({className:'piura-marker',html:'<span>USD '+price.toLocaleString('en-US')+'</span>',iconSize:[104,34],iconAnchor:[52,17]});function LocateButton(){const map=useMap();return <button className='map-locate' type='button' onClick={()=>map.locate({setView:true,maxZoom:15})} aria-label='Centrar mapa en mi ubicación'>⌖</button>}export default function PiuraMap({properties=[],onSelect}:Props){const shown=properties.length?properties:[{id:1,title:'Terreno urbano estratégico',operation:'Comprar',price:98000,coords:piura}];return <div className='leaflet-map' aria-label='Mapa interactivo de propiedades en Piura'><MapContainer center={piura} zoom={13} scrollWheelZoom className='leaflet-map-canvas'><TileLayer attribution='&copy; OpenStreetMap contributors' url='https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png'/>{shown.map(p=><Marker key={p.id} position={p.coords} icon={icon(p.price)} eventHandlers={{click:()=>onSelect?.(p)}}><Popup><strong>{p.title}</strong><br/>{p.operation} · USD {p.price.toLocaleString('en-US')}</Popup></Marker>)}<LocateButton/></MapContainer><div className='map-badge'>Mapa de Piura · {shown.length} propiedades</div></div>}
